# Environment Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/employee360

# Authentication
JWT_SECRET=your-secure-secret-key
JWT_EXPIRES_IN=30d

# Logging
LOG_LEVEL=debug

# Note: In production, replace these values with secure credentials
# Do not commit actual .env files to version control
# Environment Configuration
NODE_ENV=development  # Options: development, production, test
PORT=5000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/employee360  # Replace with your MongoDB connection string

# Authentication
JWT_SECRET=your-secure-secret-key  # Use a strong random string in production
JWT_EXPIRES_IN=30d  # Token expiration time (30 days)

# Logging
LOG_LEVEL=debug  # Options: error, warn, info, debug

# Note: In production, replace these values with secure credentials
# Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Do not commit actual .env files to version control